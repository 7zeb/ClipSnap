<Window x:Class="ClipSnap.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ClipSnap Settings" 
        Height="500" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style TargetType="CheckBox" x:Key="ToggleStyle">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
        </Style>
        
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1084D8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" x:Key="SecondaryButtonStyle">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="ClipSnap" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF"/>
            <TextBlock Text="Screenshot Tool for Windows" Foreground="#888888" FontSize="12" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,20,0,20">
            <StackPanel>
                
                <!-- Save Location -->
                <TextBlock Text="Save Location" Style="{StaticResource HeaderStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="FolderPathTextBox" 
                             Grid.Column="0"
                             Background="#2D2D2D" 
                             Foreground="#FFFFFF"
                             BorderBrush="#444444"
                             Padding="10,8"
                             FontSize="12"
                             IsReadOnly="True"/>
                    <Button Grid.Column="1" 
                            Content="Browse" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="10,0,0,0"
                            Click="BrowseFolder_Click"/>
                </Grid>
                
                <!-- Clipboard -->
                <TextBlock Text="Clipboard" Style="{StaticResource HeaderStyle}"/>
                <CheckBox x:Name="CopyToClipboardCheckBox" 
                          Content="Copy screenshot to clipboard" 
                          Style="{StaticResource ToggleStyle}"/>
                
                <!-- Hotkeys -->
                <TextBlock Text="Hotkeys" Style="{StaticResource HeaderStyle}"/>
                <CheckBox x:Name="EnableWinShiftSCheckBox" 
                          Content="Enable Win + Shift + S" 
                          Style="{StaticResource ToggleStyle}"/>
                <CheckBox x:Name="EnablePrintScreenCheckBox" 
                          Content="Enable Print Screen" 
                          Style="{StaticResource ToggleStyle}"/>
                
                <!-- Startup -->
                <TextBlock Text="Startup" Style="{StaticResource HeaderStyle}"/>
                <CheckBox x:Name="StartWithWindowsCheckBox" 
                          Content="Start ClipSnap with Windows" 
                          Style="{StaticResource ToggleStyle}"/>
                
                <!-- About -->
                <TextBlock Text="About" Style="{StaticResource HeaderStyle}"/>
                <TextBlock Text="ClipSnap v1.0.0" Foreground="#888888" FontSize="12"/>
                <TextBlock Text="A screenshot tool that automatically saves to a folder." 
                           Foreground="#666666" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap"/>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Take Screenshot" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="0,0,10,0"
                    Click="TakeScreenshot_Click"/>
            <Button Content="Save Settings" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="SaveSettings_Click"/>
        </StackPanel>
    </Grid>
</Window>